import { Placeholder } from "@/components/placeholder";

// Phase 2 replaces this with the match explorer (ranked matches across the book,
// fit/confidence/rationale, detail drawer, approve → placement).
export default function ExplorePage() {
  return (
    <Placeholder
      title="Explore"
      note="The match explorer — ranked matches across your book with fit, confidence, and approve/decline — lands next."
    />
  );
}
