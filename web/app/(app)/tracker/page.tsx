import { Placeholder } from "@/components/placeholder";

export default function TrackerPage() {
  return (
    <Placeholder
      title="Tracker"
      note="Application/placement kanban across clients (f-136). Built on the placements table the API already exposes."
    />
  );
}
