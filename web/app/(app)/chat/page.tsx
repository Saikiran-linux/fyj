import { Placeholder } from "@/components/placeholder";

export default function ChatPage() {
  return (
    <Placeholder
      title="Chat"
      note="Direct messaging with your candidates — text, files and read receipts. Lands with f-158 (messaging)."
    />
  );
}
