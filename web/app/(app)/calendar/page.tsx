import { Placeholder } from "@/components/placeholder";

// Phase 4 replaces this with the schedule (month / week / agenda of interviews,
// calls and offers derived from placements).
export default function CalendarPage() {
  return (
    <Placeholder
      title="Calendar"
      note="The schedule — interviews, calls and offers across your candidates, by month, week and agenda — lands in a later phase."
    />
  );
}
