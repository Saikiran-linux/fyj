import { Placeholder } from "@/components/placeholder";

export default function JobsPage() {
  return (
    <Placeholder
      title="Jobs"
      note="Index search over ~169k jobs via the live search_jobs RPC (f-132). Wires up here once a profile embedding is selected to query against (f-134)."
    />
  );
}
