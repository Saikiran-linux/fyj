import { Placeholder } from "@/components/placeholder";

export default function ActivityPage() {
  return (
    <Placeholder
      title="Activity"
      note="Your daily worklist — matches to review, résumés to send, replies to answer — derived from live pipeline state. Lands with f-157."
    />
  );
}
