import { Placeholder } from "@/components/placeholder";

export default function CampaignsPage() {
  return (
    <Placeholder
      title="Campaigns"
      note="One campaign per targeting profile, matched continuously against the index (f-135). Open a campaign from a client to curate its matches."
    />
  );
}
