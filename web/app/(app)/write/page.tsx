import { Placeholder } from "@/components/placeholder";

export default function WritePage() {
  return (
    <Placeholder
      title="Write"
      note="The résumé library — create, duplicate and edit résumés in a block editor with AI assists, shared with the tailoring workspace. Lands with f-156."
    />
  );
}
