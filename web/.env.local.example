# Copy to web/.env.local. The browser origin of the Worker API (Better Auth +
# tenant routes). CORS on the Worker must allow this app's origin with creds.
NEXT_PUBLIC_API_URL="http://localhost:8787"
